{
  "version": 3,
  "installCommand": "git clone https://github.com/flutter/flutter.git --branch stable --depth 1 && ./flutter/bin/flutter pub get",
  "buildCommand": "./flutter/bin/flutter build web --release --base-href /",
  "outputDirectory": "build/web",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}